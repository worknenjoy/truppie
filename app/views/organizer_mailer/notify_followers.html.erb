<!-- START MAIN CONTENT AREA -->
<tr>
  <td class="wrapper">
    <table border="0" cellpadding="0" cellspacing="0">
      <tr>
        <td>
          <p><%= t('mail-hello-message', organizer: @follower.name) %></p>
          <hr>
          <h3><%= t('mail-organizer-notify-followers-title') %></h3>
          <hr />
          <p><%= @tour.title %></p>
          <div><%= raw @tour.description %></div>
          <p><%= t('starts-in') %> <%= l(@tour.start, format: '%d de %B / %Y') %></p>
          <hr />
          <table border="0" cellpadding="0" cellspacing="0" class="btn btn-secondary">
            <tbody>
            <tr>
              <td align="left">
                <table border="0" cellpadding="0" cellspacing="0">
                  <tbody>
                  <tr>
                    <td> <a href="<%= tour_url(@tour) %>" target="_blank"><%= t('mail-organizer-see-tour') %></a> </td>
                  </tr>
                  </tbody>
                </table>
              </td>
            </tr>
            </tbody>
          </table>
        </td>
      </tr>
    </table>
  </td>
</tr>
<!-- END MAIN CONTENT AREA -->
