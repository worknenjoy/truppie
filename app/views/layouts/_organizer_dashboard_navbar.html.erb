<nav class="navbar navbar-toggleable-md navbar-light bg-faded spaced-down">
  <button class="navbar-toggler navbar-toggler-right hidden-md-up" type="button" data-toggle="collapse" data-target="#organizer-menu-collapse" aria-controls="organizer-menu-collapse" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <%= link_to organizer_path(@organizer), class: 'navbar-brand' do %>
    <i class="fa fa-map-o"></i> <%= @organizer.name %>
  <% end %>
  <div class="navbar-collapse collapse" id="organizer-menu-collapse">
    <ul class="nav navbar-nav">
      <li class="nav-item <%= "active" if current_page? action: 'manage' %>">
        <%= link_to manage_organizer_path(@organizer), class: 'nav-link' do %>
          gerenciar                
        <% end %>
      </li>
      <li class="nav-item <%= "active" if current_page? action: 'transfer' %>">
        <%= link_to transfer_organizer_path(@organizer), class: 'nav-link' do %>
          transferir               
        <% end %>
      </li>
    </ul>
    <% if @organizer.marketplace.account_missing.empty? %>
      <span class="navbar-text">
        <span class="badge badge-pill badge-success">conta verificada</span>
      </span>
    <% end %>
  </div>
</nav>