<h2>Olá, explorador(a)!</h2>

<p><%= raw(@status[:mail_first_line]) %></p>

<p><%= raw(@status[:mail_second_line]) %></p>

<%= @order.inspect %>

<% if @order.payment_method == "BOLETO" %>
  <p>Para realizar o pagamento via boleto, <a href="https://checkout.moip.com.br/boleto/<%= @order.payment %>">clique aqui para gerá-lo</a> e realizar o pagamento para confirmar sua reserva</p>
<% end %>


<h4>Temos aqui um resumo do seu pedido:</h4>

<% if !@order %>
	<p class="lead">
		<span class="label label-pill label-warning">
			Não há reservas para você neste momento
		</span>
	</p>
<% else %>
	<%= render partial: 'credit_card_status_mailer/order_table', locals: {order: @order} %>
<% end %>

<p>Para conferir o andamento do status a qualquer hora, acesse: <a href="http://www.truppie.com/orders">http://www.truppie.com/orders</a></p>

<p>Se tiver alguma dúvida, escreva pra gente: ola@truppie.com.</p> 

<p>Até mais!</p>

<p>Um abraço,</p>

<%= render partial: 'credit_card_status_mailer/footer', locals: {organizer: @organizer } %>
