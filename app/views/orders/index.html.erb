<%= notice %>
<%= render 'layouts/navbar' %>
<div class="container">
	<div class="row">
		<div class="col-xs-12">
			<h4 class="spaced-smallest">Minhas truppies</h4>
			<p>Acompanhe aqui os status das suas truppies: data, reserva, pagamento.</p>
			<% if @orders.nil? %>
				<p>Não há reservas para você</p>
			<% else %>
			<div class="main-box no-header clearfix">
                <div class="main-box-body clearfix">
                    <div class="table-responsive">
                        <table class="table user-list">
                            <thead>
                                <tr>
                                <th><span>Truppie</span></th>
                                <th><span>Data de criação</span></th>
                                <th class="text-center"><span>Status</span></th>
                                <th><span>Código do pedido</span></th>
                                <th>Valor</th>
                                </tr>
                            </thead>
                            <tbody>
								<% @orders.each do |o| %>
                                <tr>
                                    <td>
                                        <img src="<%= o.tour.photo %>" alt="<%= o.tour.title %>">
                                        <%= link_to o.tour.title, tour_path(o.tour), class: 'user-link' %>
                                        <span class="user-subhead"><%= o.tour.organizer.name %></span>
                                    </td>
                                    <td><%= o.created_at %></td>
                                    <td class="text-center">
                                        <%= raw(o.friendly_status(o.current_status)) %>
                                    </td>
                                    <td>
                                        <%= o.payment %>
                                    </td>
                                    <td >
                                       <h3><%= raw(o.tour.price) %></h3>
                                    </td>
                                </tr>
                                <% end %>
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
            <% end %>
            <p>Em caso de dúvidas ou desistência, entre em contato com a gente em <a href="mailto:ola@truppie.com">ola@truppie.com</a></p>
		</div>
	</div>
</div>
