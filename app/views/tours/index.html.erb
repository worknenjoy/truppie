<%= render 'layouts/navbar' %>
<div class="container">
	<div class="row">
		<div class="col-xs-12">
			<h3 class="spaced-smallest">Todas as truppies já criadas</h3>
			
			<table class="table user-list spaced-down">
			  <thead>
			    <tr>
			      <th colspan="3"></th>
			    </tr>
			  </thead>
			
			  <tbody>
			    <% @tours.each do |tour| %>
			      <tr>
			      	<td><%= tour.title %></td>
			        <td><%= link_to 'Exibir', tour %></td>
			        <td><%= link_to 'Editar', edit_tour_path(tour) %></td>
			        <td><%= link_to 'Apagar', tour, method: :delete, data: { confirm: 'Tem certeza que deseja apagar esta truppie?' } %></td>
			      </tr>
			    <% end %>
			  </tbody>
			</table>
			
			<%= link_to 'Criar nova truppie', new_tour_path, class: 'btn btn-primary pull-right' %>
		</div>
	</div>
</div>
